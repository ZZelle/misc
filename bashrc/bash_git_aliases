g() {
  if command -v git-number &>/dev/null; then
    git-number "$@"
  elif [ $# -eq 0 ]; then
    git status -sb
  else
    git "$@"
  fi
}

command -v _git &>/dev/null && complete -o bashdefault -o default -o nospace -F _git g
